'''Scrivi una funzione che riceva in input due liste di interi della stessa lunghezza.
La funzione deve calcolare la somma elemento per elemento e restituire una nuova lista contenente i risultati.'''

def somma_elementi(x:list, y: list) -> list[int]:
    miasomma=sum(len(x),len(y))
    return miasomma


    for i in range(x, y):
        sum
        
    return x,y
    
return [x,y]



    def get_coordinates(x:int, y:int) -> list[float]:
return [x, y]
coords = get_coordinates(12.5, 45.8)
print(coords[0], coords[1], sep=", ")
>>> 12.5, 45.8
print(type(coords)
